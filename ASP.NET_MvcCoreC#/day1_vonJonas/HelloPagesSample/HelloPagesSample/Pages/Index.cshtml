@page
@model IndexModel
@{
    //ViewData["Title"] = "Home page: " + DateTime.Now.ToLongDateString();

    ViewBag.Title = "Home BAG page"; //Das selbe wie ViewData oben

    //Punktuell das Layoutfeature ausschalten/ändern.
    //Layout = "dummy"; 
}


<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h4> @(Model.Greeting).Length</h4>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>

    <ul>
        @for (int i = 0; i < Model.Greeting.Length; i++)
        {
            //var x = i*2;
            <text> Nächstes Zeichen:</text>
            @Model.Greeting[i]
        }
    </ul>
    <form asp-action="/Index">
        <input name="name" />
        <button type="submit">Click!</button>
    </form>
</div>
